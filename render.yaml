services:
  - type: web
    name: fire-passing-project
    runtime: node
    plan: free
    buildCommand: echo "No dependencies to install"
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22
